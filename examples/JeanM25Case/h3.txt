%%%The document is sent via email from an employee using an external
%%% address to the same external address
hypothesis(h3,T,TR):-
    trace(TR),
    time(T),
    time(T1),
    email(E),
    emp(P),
    extAddr(S),
    email(E1),
    T1 < T,
    T1 >= 0,
    happens(emailSentExt2ExtS(E,m57plan,P,S),T,TR),
    not happens(recEmailReqExt2E(E1,m57plan,S,P),T1,TR).
